import {environment} from "../environments/environment";
export const AUTH_URL =  `${environment.authServerUrl}/api/login`;
export const ACCESS_TOKEN = `accessToken`;
export const EXPIRES_IN = `expires_in`;




